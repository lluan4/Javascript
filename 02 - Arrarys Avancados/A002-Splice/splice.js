const nomes  = ['Maria', 'João', 'Eduardo', 'Gabriel', 'Julia'];


// nomes.splices(índice, delete, e1, e2, e3)

// pop
// const removidos = nomes.splice(-4, 1);

// unshift
// nomes.splice(0, 0, 'Luan');

// push
// nomes.splice(nomes.length, 0, 'Luan');



console.log(nomes);